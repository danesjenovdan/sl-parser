apiVersion: v1
kind: Secret
metadata:
  name: parlaparser-slovenija-credentials
  labels:
    app.kubernetes.io/name: parlaparser-slovenija-credentials
    app: parlaparser-slovenija-credentials
stringData:
  PARSER_USER: "user"
  PARSER_PASSWORD: "password"
  API_URL: "http://localhost:8000/v3"
